apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-svc
  namespace: stagelog
spec:
  replicas: 1
  selector:
    matchLabels:
      app: auth-svc
  template:
    metadata:
      labels:
        app: auth-svc
    spec:
      containers:
      - name: auth-svc
        image: 430118823715.dkr.ecr.ap-northeast-2.amazonaws.com/stagelog-auth:latest
        imagePullPolicy: Always
        env:
        - name: DJANGO_SETTINGS_MODULE
          value: config.settings
        volumeMounts:
        - name: env-file
          mountPath: /app/.env
          subPath: .env
          readOnly: true
        ports:
        - containerPort: 80
      volumes:
      - name: env-file
        secret:
          secretName: stagelog-auth-env
          items:
          - key: .env
            path: .env
